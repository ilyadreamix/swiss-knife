jdk:
  - openjdk21
before_install:
  - sdk install gradle 9.4.0
  - sdk use gradle 9.4.0
install:
  - ./gradlew clean assemble publishToMavenLocal publishJvmPublicationToMavenLocal --no-configuration-cache
